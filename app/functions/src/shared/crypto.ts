// Stub: used later for encrypting OAuth tokens (Google refresh token).
export function encrypt(_plaintext: string): string {
  throw new Error("crypto.encrypt not implemented (Phase-0 scaffold)");
}
export function decrypt(_ciphertext: string): string {
  throw new Error("crypto.decrypt not implemented (Phase-0 scaffold)");
}
